import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miapp.calculadora',
  appName: 'calculadora-descuentos',
  webDir: 'build'
};

export default config;
